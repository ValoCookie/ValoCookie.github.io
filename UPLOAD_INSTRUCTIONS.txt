ValoCookie GitHub Pages — osu!Requests v1.5.0 website update
============================================================

Upload/replace these files in ValoCookie/ValoCookie.github.io:

  README.md
  index.html
  script.js
  osu-requests/index.html

Do NOT replace updates.json manually. The ValoCookie Release Manager owns that file.

The HTML now uses v1.5.0 as the visible fallback/current osu!Requests version.
script.js still reads updates.json + GitHub Releases, so future releases can update the displayed version automatically.

No Cloudflare Worker update is required for the v1.5.0 overlay expansion.
